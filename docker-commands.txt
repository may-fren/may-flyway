# Docker Commands - may-flyway

## Build
docker build -t ozanemrahyakupoglu/may-flyway:1.0.0 .

## Push (Docker Hub)
docker push ozanemrahyakupoglu/may-flyway:1.0.0

## Run (migrate)
docker run --rm \
  -e FLYWAY_URL="jdbc:postgresql://HOST:5432/postgres?currentSchema=MAY" \
  -e FLYWAY_USER=postgres \
  -e FLYWAY_PASSWORD=XXX \
  ozanemrahyakupoglu/may-flyway:1.0.0

---

# Docker Compose Commands - may-flyway

## Build
docker compose build

## Build (with version)
VERSION=1.0.1 docker compose build

## Push (Docker Hub)
docker compose push

## Push (with version)
VERSION=1.0.1 docker compose push

## Run (migrate)
docker compose up

## Run (with version)
VERSION=1.0.1 docker compose up
